@model Shopping.Shared.DTOs.HomePageDTO

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<div>
    <h4>HomePageDTO</h4>
    <hr />
    <dl class="dl-horizontal">
      
        @for (int i = 0; i < Model.Trending.Count(); i++)
        {
            <p>Category : @Model.Trending[i].Category.Name</p>

            <p>rank :  @Model.Trending[i].Rank</p>
            foreach(var product in  Model.Trending[i].TopProducts[0].Products)  {
                <p>Name : @product.Name</p>
            }
            
        }

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    @Html.ActionLink("Back to List", "Index")
</p>


