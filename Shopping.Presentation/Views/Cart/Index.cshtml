@model Shopping.Presentation.ViewModels.Cart

@{
    ViewBag.Title = "Index";
}


<h4>CartDTO</h4>
<hr />


@*@foreach (var item in Model.items)
            {

                @Html.BeginForm()
    {
        Html.AntiForgeryToken();

                    <div class="form-horizontal">
            <h4>Customer</h4>
            <hr />
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })


            <div class="form-group">
                @Html.LabelFor(model => model.items.FirstOrDefault(x => x.Id == item.Id).Product.Name, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.items.FirstOrDefault(x => x.Id == item.Id).Product.Name, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.items.FirstOrDefault(x => x.Id == item.Id).Product.Name, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group">
                @Html.LabelFor(model => model.items.FirstOrDefault(x => x.Id == item.Id).Quantity, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.items.FirstOrDefault(x => x.Id == item.Id).Quantity, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.items.FirstOrDefault(x => x.Id == item.Id).Quantity, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Edit" class="btn btn-default" />
                </div>
            </div>
        </div>
    }*@
}